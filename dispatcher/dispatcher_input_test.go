package dispatcher

import (
	_ "testing"
)

/*
func TestAddInputHandlerCode(t *testing.T) {
	dc := dispatcherConfig{}

	stringOne := "ABC"
	err := dc.AddInputHandlerCode(InputHandlerCode(stringOne))
	if err != nil {
		t.Errorf("Error in code: %s. Error: %s", stringOne, err.Error())
	}

	stringTwo := "DEF"
	err = dc.AddInputHandlerCode(InputHandlerCode(stringTwo))
	if err != nil {
		t.Errorf("Error with code: %s. Error: %s", stringTwo, err.Error())
	}

	stringThree := "a"
	err = dc.AddInputHandlerCode(InputHandlerCode(stringThree))
	if err == nil {
		t.Errorf("No error with code: %s. Expected error, but didn't get one.", stringThree)
	}

	stringFour := "ABC"
	err = dc.AddInputHandlerCode(InputHandlerCode(stringFour))
	if err == nil {
		t.Errorf("No error with code: %s. Expected error, but didn't get one.", stringFour)
	}
}*/
